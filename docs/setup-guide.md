# Setup Guide

Install Node.js 24 LTS and Docker Desktop. Copy `.env.example` to `.env`, replace local placeholder secrets, run `docker compose up -d`, then `npm install`. Run the web and API processes in separate terminals with `npm run dev:web` and `npm run dev:api`.
