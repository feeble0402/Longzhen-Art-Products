# Security Guideline

Enforce `BR-001`, independent administrator accounts (`BR-008`), secure password hashing, rate limits, idle session expiry, CSRF/XSS/SQL injection controls, upload validation, generic production errors and auditable privileged actions. Secrets and production data must never enter Git.
