# Deployment Guide

Provider selection is pending. The target must support containerized Node workloads, PostgreSQL, S3-compatible storage, HTTPS, daily database backup, weekly media backup and at least 30 days retention. Production accounts must remain under the owner's control.
